type User {
  id: ID! @unique
  email: String! @unique,
  name: String!,
  username: String!,
  photo: String
}

type Repository {
  id: ID! @unique
  url: String!
  healhtcheck_threshold_seconds: Number
  healthcheck_url: String
}

type Deploy {
  id: ID! @unique,
  name: String!
}