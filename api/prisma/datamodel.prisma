type User {
  id: ID! @unique
  email: String! @unique
  name: String!
  username: String!
  photo: String
}

type Project {
  id: ID! @unique
  name: String!
  repo_id: Int!
  repo_url: String!
  healthcheck_threshold_seconds: Int
  healthcheck_url: String
  retry_deploy_limit: Int
}

type Deploy {
  id: ID! @unique,
  name: String!
}